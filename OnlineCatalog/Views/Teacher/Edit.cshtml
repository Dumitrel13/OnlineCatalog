@model TeacherViewModel

@{
    ViewData["IsReadonly"] = false;
    ViewData["Model"] = "Teacher";
}

<form asp-action="Edit" asp-controller="Teacher" method="post" role="form">
    <h3 class="my-5">
        @Localizer["EditTeacher"]
    </h3>

    @Html.HiddenFor(model => model.Teacher.Id)

    <div class="col-6">
		<div class="row g-2">

			<partial name="_AddAndEdit" view-data="ViewData" for="@Model.Teacher"/>
            
            <div class="form-group">
                <label asp-for="@Model.Teacher.Email" class="form-label"></label>
                <input asp-for="@Model.Teacher.Email" name="Teacher.Email" class="form-control" />
                <span asp-validation-for="@Model.Teacher.Email" class="text-danger"></span>
            </div>

            @*<div class="form-group">
			<label asp-for="@Model.JobPeriodRoles.StartingDate" class="form-label"></label>
			<input asp-for="@Model.JobPeriodRoles.StartingDate" type="date" class="form-control" />
			<span asp-validation-for="@Model.JobPeriodRoles.StartingDate" class="text-danger"></span>
			</div>

			<div class="form-group">
			<label asp-for="@Model.JobPeriodRoles.Roles" class="control-label">Select the teacher roles</label>
			<select asp-for="@Model.SelectedRoles" asp-items="@Model.Roles" class="form-select"
			multiple="multiple">
			</select>
			<span asp-validation-for="@Model.JobPeriodRoles.Roles" class="text-danger"></span>
			</div>

            <div class="form-group">
                <label asp-for="@Model.JobPeriodSubjects.Subjects" class="control-label">Select the teacher subjects</label>
                <select asp-for="@Model.SelectedSubjects" asp-items="@Model.Subjects" class="form-select"
                        multiple="multiple">
                </select>
                <span asp-validation-for="@Model.JobPeriodSubjects.Subjects" class="text-danger"></span>
            </div>
			
            <div class="form-group">
                <label asp-for="@Model.Specialization" class="control-label">Select the teacher specializations</label>
                <select asp-for="@Model.SelectedSpecializations" asp-items="@Model.Specializations" class="form-select"
                        multiple="multiple">
                </select>
                <span asp-validation-for="@Model.Specializations" class="text-danger"></span>
            </div>*@

        </div>

		<div class="mt-3">
			<div class="col-md-offset-2 col-md-5">
				<input type="submit" class="btn btn-secondary" value="@Localizer["SaveChanges"]" />
			</div>
		</div>
	</div>

</form>