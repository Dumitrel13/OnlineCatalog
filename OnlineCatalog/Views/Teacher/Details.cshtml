@model Teacher

@{
    ViewData["IsReadonly"] = true;
}

<form asp-action="Details" asp-controller="Teacher" method="post" role="form">
    <h3 class="my-5">
        @Localizer["TeacherDetails"]
    </h3>

    <div class="col-6">
        <div class="row g-2">

            <partial name="_AddAndEdit" model="@Model" view-data="ViewData"/>
            
            <div class="form-group">
                <label asp-for="@Model.Email" class="form-label"></label>
                <input asp-for="@Model.Email" name="Teacher.Email" class="form-control" readonly=""/>
            </div>

            @*<div class="form-group">
                <label class="form-label">Teacher roles</label>
                <div class="list-group overflow-scroll border-2" style="max-height: 300px;width: auto">
                    @foreach (var jobPeriodRole in @Model.JobPeriodRoles)
                    {
                        <div class="list-group-item">
                            <partial name="_JobPeriodRolesCard" model="jobPeriodRole" />
                        </div>

                    }
                </div>
            </div>
            
            <div class="form-group">
                <label class="form-label">Teacher subjects</label>
                <div class="list-group overflow-scroll border-2" style="max-height: 300px; width: auto">
                    @foreach (var jobPeriodSubject in @Model.JobPeriodSubjects)
                    {
                        <div class="list-group-item">
                            <partial name="_JobPeriodSubjectsCard" model="jobPeriodSubject"/>
                        </div>
                    }
                </div>
            </div>
            
            <div class="form-group">
                <label class="form-label">Teacher specializations</label>
                <div class="list-group overflow-scroll border-2" style="max-height: 150px; width:200px">
                    @foreach (var specialization in @Model.Specializations)
                    {
                        <div class="list-group-item border-success">
                            <label>@specialization.Name</label>
                        </div>
                    }
                </div>
            </div>*@

        </div>

        <div class="mt-3">
            <div class="col-md-offset-2 col-md-5">
                <a class="btn btn-secondary" asp-action="Index">@Localizer["Back"]</a>
            </div>
        </div>
    </div>

</form>